server:
  port: 9092
spring:
  application:
    name: api-gateway
eureka:
  client:
    service-url:
      default-zone: http://localhost:9091/eureka
zuul:
  host:
    socket-timeout-millis: 30000
management:
  endpoints:
    web:
      exposure:
        include: "*"